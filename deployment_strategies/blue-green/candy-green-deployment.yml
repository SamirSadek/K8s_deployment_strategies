apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: candy-green
  name: candy-green
  namespace: blue-green-ns
spec:
  replicas: 4
  selector:
    matchLabels:
      app: candy-green
  template:
    metadata:
      labels:
        app: candy-green
    spec:
      containers:
      - image: samirsadek123/k8deploment:v2
        name: candy-green
        resources:
          limits:
            cpu: "500m"
            memory: "512Mi"
          requests:
            cpu: "200m"
            memory: "256Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: candy-green-deployment-service
  namespace: blue-green-ns
spec:
  selector:
    app: candy-green
  type: NodePort
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30000